# RocketSim3D Server 功能总结与 ASP.NET Core 迁移方案

本文档基于当前 `server/` 目录（Express 5）梳理现有能力，供在独立 ASP.NET Core WebAPI 项目中复现或替换。

---

## 一、整体架构

当前 `server/` 是一个 **Express 本地后端**（默认 `http://localhost:3001`），职责分为四块：


| 模块          | 是否自研  | 说明                         |
| ----------- | ----- | -------------------------- |
| 弹道计算 API 代理 | 否（转发） | 将 `/api/`* 转发到外部 AstroX 服务 |
| 用户认证        | 是     | 注册 / 登录 + JWT              |
| 火箭模板        | 是     | 读取 `public/templates/`     |
| 用户方案 CRUD   | 是     | 按用户 ID 存 JSON 文件           |
| 静态托管（可选）    | 是     | 存在 `dist/` 时托管前端 SPA       |


**开发与生产差异：**

- **开发**：Vite 代理 `/api` → `astrox.cn:8764`；`/auth`、`/templates`、`/schemes` → `:3001`
- **生产**：Express 统一提供上述全部能力（含 `/api` 反代 + 静态托管）

---

## 二、弹道 API 反向代理

**实现文件：** `server/index.js`

```
GET/POST /api/*  →  http://astrox.cn:8764/*
```

### 配置


| 环境变量            | 默认值                     | 用途          |
| --------------- | ----------------------- | ----------- |
| `ASTROX_TARGET` | `http://astrox.cn:8764` | 弹道 API 上游地址 |


### 行为要点

- `pathRewrite`：`/api/Rocket/TrajectoryOptim` → `/Rocket/TrajectoryOptim`
- **必须在 `express.json()` 之前挂载**，避免代理请求体被提前消费
- 弹道计算逻辑 **不在 server 内实现**，仅 HTTP 反向代理

### 前端调用契约

**路径：** `POST /api/Rocket/TrajectoryOptim`  
**实现：** `src/api/trajectory.js`

**请求体：**

```json
{
  "RocketInput": { },
  "Profiles": [ ],
  "RunProfiles": false,
  "GetAllData": true,
  "GetKeyData": true
}
```


| 字段                          | 说明                       |
| --------------------------- | ------------------------ |
| `RunProfiles`               | `false` = 计算；`true` = 优化 |
| `GetAllData` / `GetKeyData` | 前端固定传 `true`             |


**响应：**

- 成功：`IsSuccess: true`，含轨迹与优化结果
- 失败：`IsSuccess: false` 或 HTTP 错误；可能有 `Message`，或 ASP.NET 风格 `title` / `errors`

**前端还支持：**

- `AbortController` 终止进行中的请求（`abortTrajectoryRequest`）
- `mergeOptimizedPayload` 将 API 返回的 `RocketInput` / `Profiles` 合并回本地 payload

---

## 三、用户认证（`/auth`）

**实现文件：** `server/auth.js`、`server/middleware.js`

### 数据存储

- 路径：`server/data/users.json`
- 结构：用户数组

```json
[
  {
    "id": "uuid",
    "username": "string",
    "passwordHash": "bcrypt hash"
  }
]
```

- 密码哈希：`bcrypt`，cost = 10

### API 接口


| 方法   | 路径               | 鉴权  | 请求体                      | 成功响应                                |
| ---- | ---------------- | --- | ------------------------ | ----------------------------------- |
| POST | `/auth/register` | 无   | `{ username, password }` | `{ token, user: { id, username } }` |
| POST | `/auth/login`    | 无   | `{ username, password }` | `{ token, user: { id, username } }` |


### 校验规则

- 用户名、密码不能为空 → `400`
- 用户名至少 2 位，密码至少 4 位 → `400`
- 注册时用户名已存在 → `409`
- 登录失败（用户不存在或密码错误）→ `401`，统一文案「用户名或密码错误」

### JWT


| 项       | 值                                             |
| ------- | --------------------------------------------- |
| 库       | `jsonwebtoken`                                |
| Secret  | 环境变量 `JWT_SECRET`，默认 `rocketsim3d-dev-secret` |
| Payload | `{ id, username }`                            |
| 有效期     | 7 天                                           |


前端将 token 存入 `localStorage`（键名：`rocketsim_token`、`rocketsim_user`），见 `src/api/auth.js`。

### 鉴权中间件 `authRequired`

- Header：`Authorization: Bearer <token>`
- 失败：`401`，`{ error: "未登录" | "token 无效或已过期" }`
- 成功：`req.user = { id, username }`

---

## 四、火箭模板（`/templates`）

**实现文件：** `server/templates.js`  
**数据源：** `public/templates/index.json` + 各 `.json` 模板文件

### API 接口


| 方法  | 路径                     | 鉴权  | 说明                |
| --- | ---------------------- | --- | ----------------- |
| GET | `/templates`           | 无   | 返回模板列表            |
| GET | `/templates/:filename` | 无   | 返回完整 JSON payload |


### 列表条目结构（`index.json`）

```json
{
  "type": "CZ-2D",
  "file": "CZ2D_SSO_260601.json",
  "name": "CZ2D SSO 500km"
}
```

### 业务逻辑

- 读取 `public/templates/index.json`
- 若条目缺少 `name`，从对应 JSON 的 `RocketInput.Name` 补全
- 单文件接口防路径穿越：拒绝含 `..` 或 `/` 的 `filename`
- 读取失败 → `404`；列表读取失败 → `500`

### 模板 JSON 结构

与弹道 API 请求体一致：

```json
{
  "RocketInput": {
    "$type": "CZ-2D",
    "Name": "...",
    "...": "..."
  },
  "Profiles": [ ]
}
```

当前内置模板类型：`CZ-2D`、`CZ-4B`、`CZ-4C`、`Falcon9` 等。

---

## 五、用户方案 CRUD（`/schemes`）

**实现文件：** `server/schemes.js`  
**存储路径：** `server/data/schemes/{userId}/{schemeId}.json`  
**全部接口需 JWT 鉴权**

### API 接口


| 方法     | 路径             | 说明           |
| ------ | -------------- | ------------ |
| GET    | `/schemes`     | 当前用户方案列表（摘要） |
| POST   | `/schemes`     | 新建方案         |
| GET    | `/schemes/:id` | 读取完整方案       |
| DELETE | `/schemes/:id` | 删除方案         |


### POST 请求体

```json
{
  "name": "方案名称",
  "payload": {
    "RocketInput": { },
    "Profiles": [ ]
  }
}
```

- 缺少 `name` 或 `payload` → `400`

### 保存逻辑

1. 生成 UUID 作为 `id`
2. `type` = `payload.RocketInput.$type`（缺省为 `"Unknown"`）
3. 写入前将 `payload.RocketInput.Name` 设为方案 `name`
4. 持久化完整对象：

```json
{
  "id": "uuid",
  "name": "方案名",
  "type": "CZ-4B",
  "updatedAt": "2026-06-07T03:29:48.066Z",
  "payload": { }
}
```

### GET 列表

- 仅返回摘要：`{ id, name, type, updatedAt }`
- 按 `updatedAt` 降序排列
- 无文件时返回 `[]`

### 错误

- 方案不存在 → `404`

---

## 六、静态托管与 SPA 回退

**条件：** 存在 `../dist/`（执行 `npm run build` 后）

1. `express.static(dist)` 托管前端静态资源
2. 非 API 的 GET 请求回退到 `index.html`（SPA 路由）
3. 排除前缀：`/api`、`/auth`、`/templates`、`/schemes`

开发模式无 `dist/` 时，server 仅提供 API 与代理，前端由 Vite dev server 提供。

---

## 七、环境与依赖

### 环境变量


| 变量              | 默认值                      | 用途               |
| --------------- | ------------------------ | ---------------- |
| `PORT`          | `3001`                   | 监听端口             |
| `ASTROX_TARGET` | `http://astrox.cn:8764`  | 弹道 API 上游        |
| `JWT_SECRET`    | `rocketsim3d-dev-secret` | JWT 签名密钥（生产必须修改） |


### 其他配置

- JSON 请求体上限：`5mb`（仅作用于 `/auth`、`/templates`、`/schemes`）
- 启动命令：`npm run server` → `node server/index.js`

### 目录结构

```
server/
├── index.js          # 入口：代理 + 路由 + 静态托管
├── auth.js           # 注册 / 登录
├── schemes.js        # 用户方案 CRUD
├── templates.js      # 模板列表 / 读取
├── middleware.js     # JWT 鉴权
└── data/
    ├── users.json
    └── schemes/
        └── {userId}/
            └── {schemeId}.json
```

### 前端 API 对应关系


| 前端模块                    | 后端路径                           |
| ----------------------- | ------------------------------ |
| `src/api/auth.js`       | `/auth/login`、`/auth/register` |
| `src/api/schemes.js`    | `/templates`、`/schemes`        |
| `src/api/trajectory.js` | `/api/Rocket/TrajectoryOptim`  |


---

## 八、ASP.NET Core WebAPI 迁移建议

### 8.1 Controller 映射


| ASP.NET Controller    | 路由                                   | 说明                  |
| --------------------- | ------------------------------------ | ------------------- |
| `AuthController`      | `/auth/register`、`/auth/login`       | 注册、登录，签发 JWT        |
| `TemplatesController` | `/templates`、`/templates/{filename}` | 只读模板                |
| `SchemesController`   | `/schemes` CRUD                      | 需 `[Authorize]`     |
| 反向代理 / Yarp           | `/api/{**catch-all}`                 | 转发至 `ASTROX_TARGET` |


### 8.2 技术选型对照


| Express 现状                | ASP.NET Core 建议                                         |
| ------------------------- | ------------------------------------------------------- |
| `bcryptjs`                | `BCrypt.Net-Next` 或 `PasswordHasher<T>`                 |
| `jsonwebtoken`            | `JwtBearer` + `AddAuthentication`                       |
| `users.json`              | 初期可继续 JSON 文件；后期可换 EF Core + SQL                        |
| `schemes/{userId}/*.json` | 同上，或 Blob / 数据库                                         |
| `http-proxy-middleware`   | Yarp 或 `IHttpClientFactory` 转发                          |
| 静态 `dist/`                | 可选：`UseStaticFiles` + `MapFallbackToFile("index.html")` |


### 8.3 前端契约（建议保持不变）

前端当前直接请求以下路径，迁移时 **优先保持路径不变**，可避免修改 React 代码：

```
POST   /auth/login
POST   /auth/register
GET    /templates
GET    /templates/{filename}
GET    /schemes              （Bearer）
POST   /schemes              （Bearer）
GET    /schemes/{id}         （Bearer）
DELETE /schemes/{id}         （Bearer）
POST   /api/Rocket/TrajectoryOptim
```

若需统一 API 版本，可改为 `/api/v1/...`，但需同步修改 `src/api/*.js` 与 `vite.config.js` 代理。

### 8.4 不必迁入 WebAPI 的部分

- **弹道计算算法**：仍在 `astrox.cn:8764`（或你指定的上游），WebAPI 仅做代理，除非计划重写 AstroX 服务
- **React / Cesium / ECharts**：仍由 Vite 构建；WebAPI 是否托管 `dist/` 为可选项

### 8.5 安全注意事项

1. 生产环境必须更换 `JWT_SECRET`
2. `server/data/users.json` 含测试账户，勿将真实哈希提交到公开仓库
3. 模板接口目前无鉴权（只读公开数据，可接受）
4. 方案接口必须严格按 `userId` 隔离，禁止跨用户访问

### 8.6 建议实施顺序

1. 搭建 ASP.NET Core WebAPI 项目，配置 JWT 与 CORS（若前后端分离部署）
2. 实现 `/auth` 与 `/schemes`（可先沿用 JSON 文件存储）
3. 实现 `/templates`（读取与 Express 相同的 `public/templates/` 目录）
4. 配置 Yarp 或 HttpClient 将 `/api` 转发至 `ASTROX_TARGET`
5. 联调前端：修改 `vite.config.js` 将 `/auth`、`/templates`、`/schemes` 代理到新 WebAPI 地址
6. （可选）`UseStaticFiles` 托管 `dist/`，替代 Express 生产部署

---

## 九、联调检查清单

- `POST /auth/register` 与 `POST /auth/login` 返回 JWT
- 带 `Bearer` token 可访问 `/schemes` CRUD
- `GET /templates` 返回与 `public/templates/index.json` 一致的列表
- `POST /api/Rocket/TrajectoryOptim` 能成功代理到 AstroX 并返回 `IsSuccess: true`
- 前端「打开方案 / 保存方案 / 计算 / 优化」全流程可用

---

## 十、参考

- 现有 Express 入口：[server/index.js](server/index.js)
- 前端 API 封装：[src/api/auth.js](src/api/auth.js)、[src/api/schemes.js](src/api/schemes.js)、[src/api/trajectory.js](src/api/trajectory.js)
- 开发代理配置：[vite.config.js](vite.config.js)
- 项目说明：[AGENTS.md](AGENTS.md)

